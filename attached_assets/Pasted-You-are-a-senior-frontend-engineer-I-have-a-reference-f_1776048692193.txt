You are a senior frontend engineer. I have a reference file called ranchpad-mockup.html that contains the exact design system I want applied to the RanchPad dashboard page. Update the dashboard UI to match this mockup visually. Do not add any new features, remove any existing features, change any routes, modify any API calls, alter any state logic, or touch any backend code. Only update the visual styling and layout of the dashboard.
From the mockup, apply the following to the dashboard:
Design system — apply these everywhere:

Import Google Fonts: Fraunces (weights 400, 600, 700 including italic) and DM Sans (weights 300, 400, 500, 600)
Apply all CSS variables from the mockup's :root block exactly: colors, border radii, shadows
Use Fraunces for all headings, stat numbers, ranch name, and logo. Use DM Sans for all body text, labels, and navigation

App header:

Dark green background (#1A3628)
Top row: RanchPad logo text left (Fraunces, white, with amber accent on "Pad"), notification bell right inside a circular button with a colored dot indicator
Greeting line below in muted white, ranch name in Fraunces bold white below that
Weather strip beneath the ranch name: rounded card in rgba(255,255,255,0.1), showing weather icon, temperature in Fraunces, description text, and two detail columns (humidity and tonight's temp drop) pulled from whatever weather data is already on the page

Alert banner:

Full-width card with a red gradient background (#c0392b to #96281b), rounded corners
Left: circular icon container with warning emoji, right: bold title and subtitle text, far right: chevron arrow
This should link to wherever alerts already navigate in the app — do not change the destination

Stats row:

Three equal-width cards side by side, white background, light border, rounded corners
Each shows a large Fraunces number on top and a small all-caps label below
The alerts count card uses red for the number

Herd list:

Section header row: "Your Herd" in Fraunces left, "See all →" link right in green
Each animal row: white card with light border, animal emoji avatar in a rounded square left, name and meta info in the middle, status badge right
Status badge colors: green bg/text for healthy, red bg/text for alert, yellow bg/text for watch

Bottom navigation:

White background, light top border
Five items: Home, Herd, Alerts, Records, Account — each with an emoji icon above and label below
Active item uses dark green text with a small dot indicator beneath it
Home indicator bar at the very bottom (thin centered pill, low opacity)

General rules:

Background of the page body: #F5F1EA (warm cream)
All cards: white #FAFAF8, border-radius: 10–12px, border: 1px solid #EAF0EC
Do not change any click handlers, data fetching, conditional rendering, or auth logic
Do not change any text content that comes from real data — only update how it is displayed
If a UI element exists in the current dashboard but has no equivalent in the mockup, keep it and style it to match the overall design system as closely as possible